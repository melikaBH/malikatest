{% extends 'base.html.twig' %}

{% block content %}
    <h1>Identifiant creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('list_identifiant') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
